import './styles.css';

const Categorybadge = ({ category }) => {
  return <div className="category-container">
    <span className='category-span'>ReactJS</span>
  </div>
}

export default Categorybadge;